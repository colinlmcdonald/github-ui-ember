<h1>Orgs</h1>

<ul>
  {{#each content as |org|}}
      <li>
        <span {{action 'addToFavorites' org.id}}>[ Favorite ]</span>
        {{link-to org.id 'org' org.id}}
      </li>
  {{/each}}
</ul>

{{outlet}}
